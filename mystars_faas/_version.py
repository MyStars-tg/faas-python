"""Version + the pinned FaaS API contract version."""

__version__ = "0.1.5"

# The MyStars FaaS API contract version this SDK was built and verified against.
# Bumped in lockstep with the SDK's contract fixtures; CI fails on drift.
CONTRACT_VERSION = "1.10.0"
